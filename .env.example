# Copy to .env next to docker-compose.yml and adjust for your NAS / domain.
COLOR_MATCHER_PUBLISH_PORT=8000
# Only needed if UI and API are on different origins (usually leave empty in Docker).
COLOR_MATCHER_CORS_ORIGINS=https://color.example.com
